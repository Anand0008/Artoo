version: "3.9"

# Shared environment block — injected into every service
x-common-env: &common-env
  env_file:
    - .env
  environment:
    SQLITE_DB_PATH: /app/data/artoo.db
    ACTIVITY_LOG_PATH: /app/logs/activity.jsonl
    LLM_LOG_PATH: /app/logs/llm_calls.jsonl
    PYTHONUNBUFFERED: "1"
    PYTHONIOENCODING: "utf-8"

services:

  # ──────────────────────────────────────────────────────────────
  # Scheduler: polls Jira every N seconds, runs the full agent
  # pipeline for each "Ready for Dev" ticket found.
  # ──────────────────────────────────────────────────────────────
  artoo_scheduler:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: artoo_scheduler
    restart: unless-stopped
    <<: *common-env
    volumes:
      - artoo_data:/app/data
      - artoo_logs:/app/logs
      # Mount AWS credentials read-only (needed only when LLM_PROVIDER=bedrock)
      - ${HOME}/.aws:/root/.aws:ro
    networks:
      - artoo_net
    depends_on:
      artoo_metrics:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "from scheduler.poller import is_scheduler_running; assert is_scheduler_running()"]
      interval: 60s
      timeout: 15s
      retries: 3
      start_period: 30s

  # ──────────────────────────────────────────────────────────────
  # Metrics API: FastAPI server on port 8080
  #   GET  /health            — liveness check
  #   GET  /metrics           — JSON KPI report
  #   GET  /docs              — Swagger UI
  #   POST /pr/{run_id}/approve  — mark PR approved (requires X-Api-Key)
  #   POST /pr/{run_id}/reject   — mark PR rejected (requires X-Api-Key)
  # ──────────────────────────────────────────────────────────────
  artoo_metrics:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: artoo_metrics
    command: ["python", "main.py", "--mode", "metrics-server"]
    restart: unless-stopped
    <<: *common-env
    environment:
      SQLITE_DB_PATH: /app/data/artoo.db
      METRICS_PORT: "8080"
    ports:
      - "8080:8080"
    volumes:
      - artoo_data:/app/data
      - artoo_logs:/app/logs:ro
    networks:
      - artoo_net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

  # ──────────────────────────────────────────────────────────────
  # Dashboard: Streamlit UI on port 8501
  #   http://localhost:8501  — KPI cards, workflow runs, LLM call log,
  #                            PR approve/reject actions
  # ──────────────────────────────────────────────────────────────
  artoo_dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: artoo_dashboard
    command: ["streamlit", "run", "dashboard.py",
              "--server.port", "8501",
              "--server.address", "0.0.0.0",
              "--server.headless", "true",
              "--browser.gatherUsageStats", "false"]
    restart: unless-stopped
    <<: *common-env
    environment:
      SQLITE_DB_PATH: /app/data/artoo.db
      METRICS_API_URL: http://artoo_metrics:8080
    ports:
      - "8501:8501"
    volumes:
      - artoo_data:/app/data
      - artoo_logs:/app/logs:ro
    networks:
      - artoo_net
    depends_on:
      artoo_metrics:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8501/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

volumes:
  artoo_data:
    driver: local
  artoo_logs:
    driver: local

networks:
  artoo_net:
    driver: bridge
